{
  "name": "bubble-debate-backend",
  "version": "1.0.0",
  "description": "Standalone backend for the Bubble Observatory multi-agent debate engine. Replaces Claude Artifacts' in-browser API access with a real server-side orchestrator.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  }
}
